---
import Base from '../layouts/Base.astro';
import Gallery from '../components/Gallery.astro';
import Icon from '../components/Icon.astro';
import { withBase } from '../utils/url';

/* Phase 2 landing per SH_Web_Site_Copy.md § 0.1 + SH_Web_Visualization_List
   § 1.1 + 1.2. Hero rotace 5 záběrů (Foto se připravuje, zatím placeholdery),
   „Co Hub nabízí" 4 karty s horizontal scroll-snap gallery, section teasery
   pro 8 sekcí, masterplan, finální CTA panel.

   Hero rotace: cross-fade přes všech 5 slidů, 5 s na slide, 25 s celkový cyklus.
   `prefers-reduced-motion: reduce` zastaví na slide 1. */

/* === HERO ROTACE (5 záběrů) === */
/* Per Visualization_List_Cleanup.md § Landing/Hero h01-h05:
   h01 Budova C zvenku · h02 Vnitřní dvůr · h03 Pokoj 2+kk s pracujícím ·
   h04 Kapsle interiér · h05 Areál Klecany aerial. Nové Tier A rendery z
   vault Visualizations/h*. */
const heroSlides = [
  {
    src: withBase('/images/hub/hero/h01.jpg'),
    alt: 'Budova C (coworking + komunita) v parteru, denní pohled z odstupu, kreativní atmosféra areálu.',
  },
  {
    src: withBase('/images/hub/hero/h02.jpg'),
    alt: 'Vnitřní dvůr areálu za modré hodiny, lidé u stolu venku, světla v oknech budov.',
  },
  {
    src: withBase('/images/hub/hero/h03.jpg'),
    alt: 'Privátní apartmán 2+kk: jeden člověk u laptopu u okna s denním světlem, káva na stole, klidná pracovní atmosféra.',
  },
  {
    src: withBase('/images/hub/hero/h04.jpg'),
    alt: 'Velký pokoj s privátními kapslemi v rohu, „obývák s pody" se sedáky, knihami a převlékací kabinkou v pozadí.',
  },
  {
    src: withBase('/images/hub/hero/h05.jpg'),
    alt: 'Letecký pohled na horní kasárny Klecan v ranní mlze, krajinný kontext nad Vltavou.',
  },
];

/* === AMENITY ZIG-ZAG (4 řady) === */
/* Zig-zag layout per Visualization_List_Cleanup.md § Landing/Co Hub nabízí.
   Galerie reálných záběrů per řada (Gallery komponenta: dot pagination +
   chevron buttons + scroll-snap), kde vault folder zatím nemá záběr, slot
   nese placeholder s krátkou poznámkou, co tam bude. Copy per Site_Copy
   § 0.4.1. */
type GalleryItem =
  | { src: string; alt: string }
  | { placeholder: string; alt: string };
type AmenityRow = {
  eyebrow: string;
  lede: string;
  href: string;
  detailLabel: string;
  gallery: GalleryItem[];
};

const amenityRows: AmenityRow[] = [
  {
    eyebrow: 'Ubytování',
    lede: 'Devět typů, od privátní kapsle pro jednoho po byt 5+kk pro skupinu.',
    href: '/ubytovani/',
    detailLabel: 'Možnosti ubytování',
    /* Vault neukládá samostatné Ubytování záběry pro landing, recyklujeme
       z konkrétních ubytovacích sekcí (ks, kd, lj, lp, p1k a další zatím
       empty), držíme aktuální mix. */
    gallery: [
      { src: withBase('/images/hub/interior/1kk-a4-a6.jpg'), alt: '1+kk ranní rituál: káva u okna, denní světlo, postel ustlaná v pozadí.' },
      { src: withBase('/images/hub/interior/vetsi-pokoj-a.jpg'), alt: 'Větší privátní apartmán pro skupinu: jídelní stůl, kuchyňský kout, postel.' },
      { src: withBase('/images/hub/amenities/kapsle-horizontal-2.jpg'), alt: 'Kapsle ve společném pokoji s knihovnou a rostlinou.' },
      { src: withBase('/images/hub/amenities/1kk-2.jpg'), alt: '1+kk varianta s manželskou postelí a pracovním stolem u okna.' },
    ],
  },
  {
    eyebrow: 'Coworking',
    lede: 'Volný stůl v sále zdarma pro rezidenty. Fixní stůl, ateliéry, dílny s těžkými stroji za hodinovou sazbu.',
    href: '/coworking/',
    detailLabel: 'Coworking, dílny a ateliéry',
    /* Coworking žije v budově C. Existující Building C záběry (exterior +
       interior) drží 2 první sloty, zbývající cw01-cw04 specifické záběry
       z vault Visualizations/cw zatím chybí (úplně nové vizualizace per
       Marek + Scott Weber / Impact Hub reference). */
    gallery: [
      { src: withBase('/images/hub/amenities/cw-budova-c-exterior.jpg'), alt: 'Budova C zvenku, parter s hlavním vstupem do coworkingu a komunitního zázemí.' },
      { src: withBase('/images/hub/amenities/cw-budova-c-interior.jpg'), alt: 'Interiér budovy C, společný prostor pro coworking a komunitní akce.' },
      { placeholder: 'cw01 Volný stůl v sále, hot-desking, 6 až 8 lidí ve flow', alt: 'Coworkingový sál v 1. NP budovy C, flexibilní hot-desking.' },
      { placeholder: 'cw03 Zasedací místnost s týmem 4 až 6 lidí, prezentace na obrazovku', alt: 'Zasedací místnost v budově C v plné akci.' },
      { placeholder: 'cw04 Ateliér nebo dílna v D1 nebo H1 až H3, hardware a nářadí', alt: 'Ateliér nebo dílna s fyzickou prací mimo office.' },
    ],
  },
  {
    eyebrow: 'Komunita',
    lede: 'Wellness, sport, gastro, kultura. Bazén, sauna, kantýna v budově B, klubovna, komunitní zahrádky.',
    href: '/komunita/',
    detailLabel: 'Komunitní program',
    /* Vault ko02-ko04 nové (Bazén, Kantýna, Klubovna). ko01 Grilovací
       místo zatím chybí. */
    gallery: [
      { src: withBase('/images/hub/amenities/ko02-bazen.jpg'), alt: 'Bazén v centrálním objektu areálu, letní odpoledne, sluneční terasa, relax po práci.' },
      { src: withBase('/images/hub/amenities/ko04-klubovna.jpg'), alt: 'Klubovna v budově C s eventem, lidé u stolů, kulturní život uvnitř Hubu.' },
      { src: withBase('/images/hub/exterior/trznice-pivovar.jpg'), alt: 'Tržnice s minipivovarem v budově B, večerní zázemí pro gastronomii.' },
      { placeholder: 'ko01 Grilovací místo, rodinné grilování v sobotu poledne, 8 až 12 lidí', alt: 'Grilovací místo v komunitním parku v sobotní poledne.' },
    ],
  },
  {
    eyebrow: 'Doprava',
    lede: 'Hub-shuttle do metra Kobylisy, 5 jízd v ceně. Bus 374 a Hub-taxi. Cyklostezka EuroVelo 7 podél Vltavy.',
    href: '/doprava/',
    detailLabel: 'Jak se sem dostanete',
    /* Vault dp02 nový (cyklostezka). dp01 Hub-shuttle a dp03 zastávka
       autobusu zatím chybí. */
    gallery: [
      { src: withBase('/images/hub/amenities/dp02-cyklostezka.jpg'), alt: 'Cyklista v motion bluru na cyklostezce Praha až Klecany, alternativa pro tělo a klima.' },
      { src: withBase('/images/hub/amenities/doprava-tram.jpg'), alt: 'Plánovaná tramvajová zastávka Výzkumný ústav I poblíž areálu (2029 až 2030).' },
      { placeholder: 'dp01 Hub-shuttle dodávka před hlavním vstupem, 3 až 4 rezidenti nastupují', alt: 'Hub-shuttle dodávka před hlavním vstupem do Hubu, ranní mlha.' },
      { placeholder: 'dp03 Zastávka autobusu 374 v Klecanech, U hřbitova', alt: 'Zastávka autobusu 374 jako záložní spoj na Kobylisy.' },
    ],
  },
];

/* === NAV STRIP (pod hero) === */
const navItems = [
  { path: '/rezervace/',  label: 'Rezervace',  icon: 'calendar-check', primary: true },
  { path: '/ubytovani/',  label: 'Ubytování',  icon: 'bed-double' },
  { path: '/coworking/',  label: 'Coworking',  icon: 'briefcase' },
  { path: '/komunita/',   label: 'Komunita',   icon: 'users' },
  { path: '/okoli/',      label: 'Okolí',      icon: 'map-pin' },
  { path: '/doprava/',    label: 'Doprava',    icon: 'bus' },
  { path: '/stipendia/',  label: 'Stipendia',  icon: 'sparkles' },
];
---
<Base
  title="Startovací Hub Klecany · Bydlení a coworking 15 min od Prahy"
  description="Devět typů bydlení od kapsle po byt 5+kk, coworking, dílny, sauna a kavárna. Horní kasárna Klecany, patnáct minut autobusem od metra Kobylisy. Od 3 000 Kč měsíčně, energie a internet v ceně."
  headerVariant="light">

  {/* ============ HERO ROTATION ============ */}
  <section class="lp-hero" aria-label="Startovací Hub Klecany, hero galerie">
    <div class="lp-hero__rotation" aria-hidden="true">
      {heroSlides.map((slide, i) => (
        <div class="lp-hero__slide" data-slide={i} style={`animation-delay: ${i * 5 - 1}s`}>
          <img class="lp-hero__bg" src={slide.src} alt={slide.alt} loading={i === 0 ? 'eager' : 'lazy'} decoding="async" />
        </div>
      ))}
    </div>
    <div class="lp-hero__scrim" aria-hidden="true"></div>

    <div class="lp-hero__inner">
      <h1 class="lp-hero__title">Startovací Hub.</h1>
      <p class="lp-hero__lede">Na prahu metropole · Praha-sever</p>
    </div>
  </section>

  {/* ============ NAV STRIP ============ */}
  <nav class="lp-nav" aria-label="Hlavní sekce">
    <div class="lp-nav__inner">
      {navItems.map((item) => (
        <a class:list={['lp-nav__item', item.primary && 'lp-nav__item--primary']} href={withBase(item.path)}>
          <Icon name={item.icon} size={15} strokeWidth={1.75} />
          {item.label}
        </a>
      ))}
    </div>
  </nav>

  {/* ============ AMENITY ZIG-ZAG (4 řady) ============ */}
  <section class="lp-amenities" aria-label="Nabídka Hubu">
    <div class="lp-amenities__inner">
      <div class="lp-zigzag">
        <div class="lp-zigzag__col">
          {amenityRows.filter((_, i) => i % 2 === 0).map((row) => (
            <div class="lp-zigzag__card">
              <a class="lp-zigzag__cta" href={withBase(row.href)}>
                {row.detailLabel}<span aria-hidden="true"> →</span>
              </a>
              <Gallery images={row.gallery} ariaLabel={row.eyebrow} aspectRatio="4 / 3" />
            </div>
          ))}
        </div>
        <div class="lp-zigzag__col lp-zigzag__col--offset">
          {amenityRows.filter((_, i) => i % 2 === 1).map((row) => (
            <div class="lp-zigzag__card">
              <a class="lp-zigzag__cta" href={withBase(row.href)}>
                {row.detailLabel}<span aria-hidden="true"> →</span>
              </a>
              <Gallery images={row.gallery} ariaLabel={row.eyebrow} aspectRatio="4 / 3" />
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  {/* ============ MÍSTO / MASTERPLAN ============ */}
  <section class="lp-place" aria-label="Místo, kde Hub stojí">
    <div class="lp-place__inner">
      <div class="lp-place__text">
        <p class="lp-place__eyebrow">Místo</p>
        <h2 class="lp-place__title">Horní kasárny, 15 km severně od centra Prahy.</h2>
        <p>
          Areál horních kasáren v Klecanech leží nad pravým břehem Vltavy. Patnáct kilometrů
          severně od Václavského náměstí, dvanáct minut autobusem 374 od metra Kobylisy.
        </p>
        <p>
          Bývalý vojenský areál, který otevíráme po etapách od roku 2026. Hub je první obyvatelná
          fáze záměru VPD1.
        </p>
        <p class="lp-place__links">
          <a href={withBase('/okoli/')}>Co je v okolí →</a>
          <a href={withBase('/doprava/')}>Jak se sem dostanete →</a>
        </p>
      </div>
      <figure class="lp-place__map">
        <img
          src={withBase('/images/hub/masterplan_fotoreal.jpg')}
          alt="Fotorealistický masterplan areálu Horních kasáren, popisky budov A1 až A6, B, C, D, E, F, P, S a venkovního bazénu."
          loading="lazy"
          decoding="async" />
        <figcaption>Plán areálu</figcaption>
      </figure>
    </div>
  </section>

  {/* ============ MAPA ============ */}
  <section class="lp-map-strip" aria-label="Poloha areálu na mapě">
    <div class="lp-map-strip__inner">
      <div class="lp-map-strip__frame" id="lp-landing-map" role="application" aria-label="Interaktivní mapa areálu Horních kasáren v Klecanech"></div>
      <p class="lp-map-strip__address">
        <strong>Horní kasárna Klecany</strong>
        <a href="https://maps.app.goo.gl/vCF6VbW7Z49sggau6" target="_blank" rel="noopener">Otevřít v Mapách →</a>
      </p>
    </div>
  </section>

  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" crossorigin="anonymous" />
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" crossorigin="anonymous" is:inline></script>

  {/* ============ FINÁLNÍ CTA PANEL ============ */}
  <section class="lp-cta" aria-label="Závěrečná výzva k registraci">
    <div class="lp-cta__inner">
      <p class="lp-cta__eyebrow">Rezervujte si místo</p>
      <h2 class="lp-cta__title">Bydlete tady měsíc, nebo rok.</h2>
      <p class="lp-cta__lede">
        Rezervace je nezávazná. Vyplníte konfiguraci pokoje, datum nástupu a kontakt.
        Z rezervačního oddělení vám zavoláme do 24 hodin s detailem dostupnosti a ceny.
      </p>
      <a class="lp-cta__button" href={withBase('/rezervace/')}>Rezervace</a>
    </div>
  </section>
</Base>

<script is:inline>
  document.addEventListener('astro:page-load', () => {
    const mapEl = document.getElementById('lp-landing-map');
    if (!mapEl || mapEl.dataset.mapBound || typeof L === 'undefined') return;
    mapEl.dataset.mapBound = '1';
    const center = [50.181447059112294, 14.424094125168379];
    const map = L.map(mapEl, {
      center, zoom: 16,
      zoomControl: false, attributionControl: false,
      scrollWheelZoom: false,
    });
    L.tileLayer('https://{s}.basemaps.cartocdn.com/rastertiles/voyager_nolabels/{z}/{x}/{y}{r}.png', {
      attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> &copy; <a href="https://carto.com/">CARTO</a>',
      subdomains: 'abcd', maxZoom: 19,
    }).addTo(map);
    const icon = L.divIcon({
      html:
        '<svg width="30" height="40" viewBox="0 0 28 36" fill="none" xmlns="http://www.w3.org/2000/svg">' +
        '<path d="M14 0C6.27 0 0 6.27 0 14c0 9.33 14 22 14 22S28 23.33 28 14C28 6.27 21.73 0 14 0z" fill="#5A2A5F"/>' +
        '<circle cx="14" cy="14" r="5" fill="#ffffff"/>' +
        '</svg>',
      className: '', iconSize: [30, 40], iconAnchor: [15, 40], popupAnchor: [0, -42],
    });
    const gmaps = 'https://maps.app.goo.gl/vCF6VbW7Z49sggau6';
    const popupHtml =
      '<a href="' + gmaps + '" target="_blank" rel="noopener" class="lp-map-popup">' +
        '<strong>Horní kasárna Klecany</strong>' +
        '<span class="lp-map-popup__cta">Otevřít v Mapách →</span>' +
      '</a>';
    L.marker(center, { icon })
      .addTo(map)
      .bindPopup(popupHtml, { className: 'lp-map-popup-wrap' })
      .openPopup()
      .on('click', () => window.open(gmaps, '_blank'));
    L.control.attribution({ prefix: false }).addTo(map);
    L.control.zoom({ position: 'bottomright' }).addTo(map);
    mapEl.addEventListener('click', () => map.scrollWheelZoom.enable(), { once: true });
  });
</script>

<style>
  /* ============ HERO ROTATION ============ */
  /* 5 slides absolutely overlapped, 25 s cyklus, 5 s na slide,
     cross-fade 1 s. animation-delay posunuje každý slide v cyklu. */
  .lp-hero {
    position: relative;
    isolation: isolate;
    min-height: 64vh;
    display: grid;
    align-items: end;
    padding: var(--space-8) var(--gutter) var(--space-7);
    color: #fff;
    background: var(--k-100);
    overflow: hidden;
  }
  .lp-hero__rotation {
    position: absolute;
    inset: 0;
    z-index: -2;
  }
  /* 5 slidů jako horizontal carousel: 4 s nehybný hold, pak 1 s swipe
     celé fotky doleva. Sousední slide zároveň zprava vplouvá dovnitř,
     takže přechod je jeden plynulý sweep bez cross-fade.
     Slide N animation-delay = N × 5 s − 1 s (pre-roll, aby slide 0 byl
     hned na svém místě při page loadu). */
  .lp-hero__slide {
    position: absolute;
    inset: 0;
    transform: translateX(100%);
    animation: lp-hero-swipe 25s cubic-bezier(0.65, 0, 0.35, 1) infinite;
    will-change: transform;
  }

  @keyframes lp-hero-swipe {
    /* 25 s smyčka = 100 %. 0-4 % (1 s): swipe-in zprava. 4-20 % (4 s):
       hold na středu. 20-24 % (1 s): swipe-out doleva. 24-100 %: hidden
       off-screen vlevo dokud se animace neresetuje. Boundary teleport
       z -100 % zpět na 100 % se děje off-screen, uživatel ho nevidí. */
    0%   { transform: translateX(100%); }
    4%   { transform: translateX(0); }
    20%  { transform: translateX(0); }
    24%  { transform: translateX(-100%); }
    100% { transform: translateX(-100%); }
  }

  .lp-hero__bg {
    position: absolute;
    inset: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    filter: brightness(0.88);
  }
  .lp-hero__scrim {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to top,
      rgba(0,0,0,0.75) 0%,
      rgba(0,0,0,0.45) 35%,
      rgba(0,0,0,0.05) 75%
    );
    z-index: -1;
  }
  .lp-hero__inner {
    max-width: var(--container);
    margin: 0 auto;
    width: 100%;
  }
  .lp-hero__title {
    font: 600 clamp(2.5rem, 6vw, 4.75rem) / 1.05 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    margin: 0 0 var(--space-2);
    max-width: 18ch;
  }
  .lp-hero__lede {
    font: 600 var(--text-2xs) / 1 var(--font-sans);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    margin: 0;
    color: rgba(255,255,255,0.7);
  }

  /* reduced-motion: freeze on slide 0 */
  @media (prefers-reduced-motion: reduce) {
    .lp-hero__slide { animation: none; transform: translateX(-100%); }
    .lp-hero__slide[data-slide="0"] { transform: translateX(0); }
  }

  /* ============ AMENITY ZIG-ZAG ============ */
  .lp-amenities {
    padding: var(--space-7) var(--gutter) var(--space-6);
    background: var(--bg);
    color: var(--fg);
    overflow: hidden;
  }
  .lp-amenities__inner {
    max-width: var(--container-wide);
    margin: 0 auto;
  }

  .lp-zigzag {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: clamp(24px, 3vw, 40px);
  }
  .lp-zigzag__col {
    display: flex;
    flex-direction: column;
    gap: clamp(32px, 4vw, 56px);
    min-width: 0;
  }
  .lp-zigzag__col--offset {
    padding-top: clamp(100px, 14vw, 220px);
  }
  .lp-zigzag__card {
    display: grid;
    gap: var(--space-2);
    min-width: 0;
  }
  .lp-zigzag__cta {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 16px;
    font: 600 var(--text-xs) / 1 var(--font-sans);
    letter-spacing: var(--tracking-wide);
    text-transform: uppercase;
    color: var(--accent);
    background: transparent;
    border: 1px solid var(--accent);
    border-radius: var(--radius-input);
    text-decoration: none;
    width: fit-content;
    transition: background 200ms ease, color 200ms ease;
  }
  .lp-zigzag__cta:hover,
  .lp-zigzag__cta:focus-visible {
    background: var(--accent);
    color: var(--accent-on);
    outline: none;
  }

  @media (max-width: 760px) {
    .lp-zigzag {
      grid-template-columns: 1fr;
    }
    .lp-zigzag__col--offset {
      padding-top: 0;
    }
  }

  /* ============ NAV STRIP ============ */
  .lp-nav {
    padding: var(--space-4) var(--gutter);
    background: var(--bg);
    border-bottom: 1px solid var(--border-muted);
  }
  .lp-nav__inner {
    max-width: var(--container);
    margin: 0 auto;
    display: flex;
    gap: var(--space-2);
    justify-content: center;
    flex-wrap: wrap;
  }
  .lp-nav__item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 10px 18px;
    font: 600 var(--text-sm) / 1 var(--font-sans);
    color: var(--fg);
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-input);
    text-decoration: none;
    white-space: nowrap;
    transition: border-color 200ms ease, color 200ms ease, background 200ms ease;
  }
  .lp-nav__item :global(svg) {
    flex-shrink: 0;
  }
  .lp-nav__item:hover,
  .lp-nav__item:focus-visible {
    border-color: var(--accent);
    color: var(--accent);
    outline: none;
  }
  .lp-nav__item--primary {
    background: var(--accent);
    color: var(--accent-on);
    border-color: var(--accent);
  }
  .lp-nav__item--primary:hover,
  .lp-nav__item--primary:focus-visible {
    background: #fff;
    color: var(--accent);
    border-color: var(--accent);
  }

  @media (max-width: 760px) {
    .lp-nav__inner {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: var(--space-1);
    }
    .lp-nav__item {
      padding: 8px 4px;
      font-size: var(--text-2xs);
      justify-content: center;
      gap: 3px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      min-width: 0;
    }
    .lp-nav__item :global(svg) {
      width: 14px;
      height: 14px;
    }
  }

  /* ============ MÍSTO / MASTERPLAN ============ */
  .lp-place {
    padding: var(--space-8) var(--gutter);
    background: var(--bg-muted);
    color: var(--fg);
  }
  .lp-place__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-7);
    max-width: var(--container-wide);
    margin: 0 auto;
    align-items: center;
  }
  .lp-place__eyebrow {
    font: 600 var(--text-2xs) / 1 var(--font-sans);
    color: var(--accent);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    margin: 0 0 var(--space-3);
  }
  .lp-place__title {
    font: 600 clamp(1.75rem, 3.5vw, 2.5rem) / 1.15 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    margin: 0 0 var(--space-5);
    max-width: 22ch;
  }
  .lp-place__text p {
    line-height: 1.6;
    margin: 0 0 var(--space-4);
    max-width: 50ch;
  }
  .lp-place__links {
    display: flex;
    gap: var(--space-5);
    flex-wrap: wrap;
    margin-top: var(--space-5);
  }
  .lp-place__links a {
    color: var(--accent);
    text-decoration: none;
    border-bottom: 1px solid var(--accent-rule);
    padding-bottom: 2px;
    font: 600 var(--text-sm) / 1 var(--font-sans);
    transition: border-color 160ms ease;
  }
  .lp-place__links a:hover { border-color: var(--accent); }
  .lp-place__map {
    margin: 0;
  }
  .lp-place__map figcaption {
    font: 600 var(--text-2xs) / 1 var(--font-sans);
    color: var(--fg-subtle);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    margin-top: var(--space-3);
    text-align: center;
  }
  .lp-place__map img {
    width: 100%;
    height: auto;
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-input);
    display: block;
  }

  @media (max-width: 760px) {
    .lp-place__inner { grid-template-columns: 1fr; gap: var(--space-6); }
  }

  /* ============ FINÁLNÍ CTA ============ */
  .lp-cta {
    padding: var(--space-8) var(--gutter);
    background: var(--accent);
    color: #fff;
    text-align: center;
  }
  .lp-cta__inner {
    max-width: 640px;
    margin: 0 auto;
  }
  .lp-cta__eyebrow {
    font: 600 var(--text-2xs) / 1 var(--font-sans);
    color: rgba(255,255,255,0.85);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    margin: 0 0 var(--space-3);
  }
  .lp-cta__title {
    font: 600 clamp(1.75rem, 3.5vw, 2.5rem) / 1.15 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    margin: 0 0 var(--space-4);
  }
  .lp-cta__lede {
    font-size: var(--text-md);
    line-height: 1.5;
    margin: 0 0 var(--space-6);
    opacity: 0.92;
  }
  .lp-cta__button {
    display: inline-block;
    padding: 14px 32px;
    font: 700 var(--text-md) / 1 var(--font-sans);
    color: var(--accent);
    background: #fff;
    border: 1px solid #fff;
    border-radius: var(--radius-input);
    text-decoration: none;
    transition: background 200ms ease, color 200ms ease;
  }
  .lp-cta__button:hover {
    background: transparent;
    color: #fff;
  }

  /* ============ MAPA / KLECANY NA MAPĚ ============ */
  /* Full-width Leaflet strip mezi masterplanem a finálním CTA. */
  .lp-map-strip {
    padding: var(--space-9) var(--gutter);
    background: var(--bg);
    color: var(--fg);
  }
  .lp-map-strip__inner {
    max-width: var(--container-wide);
    margin: 0 auto;
  }
  .lp-map-strip__frame {
    position: relative;
    width: 100%;
    height: clamp(320px, 44vh, 460px);
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-input);
    overflow: hidden;
    background: var(--k-05);
  }
  .lp-map-strip__address {
    margin: var(--space-4) 0 0;
    font: 400 var(--text-sm) / 1.55 var(--font-sans);
    color: var(--fg-muted);
  }
  .lp-map-strip__address strong { color: var(--k-100); font-weight: 600; }
  .lp-map-strip__address a {
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
    margin-left: var(--space-3);
  }
  .lp-map-strip__address a:hover { text-decoration: underline; }

  /* Leaflet brand overrides — same plum accent, sharp borders. */
  :global(.leaflet-container) {
    background: var(--k-05);
    font-family: var(--font-sans);
    z-index: 0;
  }
  :global(.leaflet-control-attribution) {
    background: rgba(255, 255, 255, 0.92) !important;
    color: var(--fg-muted) !important;
    font-size: 10px !important;
    border-top: 1px solid var(--border-muted) !important;
    padding: 2px 8px !important;
  }
  :global(.leaflet-control-attribution a) { color: var(--fg) !important; }
  :global(.leaflet-control-zoom a) {
    background: var(--bg) !important;
    color: var(--fg-muted) !important;
    border-color: var(--border-muted) !important;
    font-family: var(--font-sans);
  }
  :global(.leaflet-control-zoom a:hover) {
    background: var(--accent) !important;
    color: #fff !important;
    border-color: var(--accent) !important;
  }
  :global(.lp-map-popup-wrap .leaflet-popup-content-wrapper) {
    background: var(--bg);
    border: 1px solid var(--accent);
    box-shadow: 0 8px 32px rgba(90, 42, 95, 0.18);
    border-radius: var(--radius-input);
    padding: 0;
  }
  :global(.lp-map-popup-wrap .leaflet-popup-tip) { background: var(--bg); border: 1px solid var(--accent); }
  :global(.lp-map-popup-wrap .leaflet-popup-content) { margin: 0; width: auto !important; min-width: 200px; }
  :global(.lp-map-popup-wrap .leaflet-popup-close-button) { color: var(--fg-subtle) !important; padding: 6px 8px 0 0 !important; }
  :global(.lp-map-popup-wrap .leaflet-popup-close-button:hover) { color: var(--accent) !important; }
  :global(.lp-map-popup) {
    display: grid; gap: 4px;
    padding: 14px 16px;
    color: var(--k-100);
    font: 500 13px / 1.5 var(--font-sans);
    text-decoration: none; cursor: pointer;
  }
  :global(.lp-map-popup strong) { font-weight: 600; color: var(--k-100); letter-spacing: 0.02em; }
  :global(.lp-map-popup .lp-map-popup__cta) { color: var(--accent); font-weight: 600; margin-top: 4px; }
</style>
