---
import Base from '../layouts/Base.astro';
import Gallery from '../components/Gallery.astro';
import Icon from '../components/Icon.astro';
import Arrow from '../components/Arrow.astro';
import BrandLogo from '../components/BrandLogo.astro';
import ResidentBenefits from '../components/ResidentBenefits.astro';
import CrossLinkPair from '../components/CrossLinkPair.astro';
import { withBase } from '../utils/url';

/* Phase 2 landing per SH_Web_Site_Copy.md § 0.1 + SH_Web_Visualization_List
   § 1.1 + 1.2. Hero rotace 4 záběrů (Tier A rendery z vault hero-update),
   „Co Hub nabízí" 4 karty s horizontal scroll-snap gallery, section teasery
   pro 8 sekcí, masterplan, finální CTA panel.

   Hero rotace: swipe přes 4 slidy, 5 s na slide, 20 s celkový cyklus.
   `prefers-reduced-motion: reduce` zastaví na slide 1. */

/* === HERO ROTACE (4 záběry) === */
/* Tier A rendery z vault Visualizations/h - Hero/hero-update.
   Každý slide má desktop (landscape) a phone (portrait) variantu,
   <picture> přepíná v zlomu 768 px. */
const heroSlides = [
  {
    desktop: withBase('/images/hub/hero/h00.jpg'),
    phone:   withBase('/images/hub/hero/h00-phone.jpg'),
    alt: 'Startovací Hub Klecany, hero úvod.',
  },
  {
    desktop: withBase('/images/hub/hero/h01.jpg'),
    phone:   withBase('/images/hub/hero/h01-phone.jpg'),
    alt: 'Privátní apartmány v parteru po setmění, lidé před vstupem mezi břízami, teplá světla v oknech.',
  },
  {
    desktop: withBase('/images/hub/hero/h02.jpg'),
    phone:   withBase('/images/hub/hero/h02-phone.jpg'),
    alt: 'Chodba s privátními kapslemi v dřevěném obložení, lavice a rostlina v lounge, tlumené ambientní světlo.',
  },
  {
    desktop: withBase('/images/hub/hero/h03.jpg'),
    phone:   withBase('/images/hub/hero/h03-phone.jpg'),
    alt: 'Vnitřní dvůr areálu za modré hodiny, lidé u dlouhého stolu venku, světla v oknech renovovaných budov.',
  },
  {
    desktop: withBase('/images/hub/hero/h04.jpg'),
    phone:   withBase('/images/hub/hero/h04-phone.jpg'),
    alt: 'Privátní apartmán 1+kk: žena vaří v kuchyni, muž čte u stolu, postel u okna, teplé večerní světlo.',
  },
];

/* === SEKCE ZIG-ZAG (5 řad, one-pager) === */
/* Každá sekce má anchor id pro scroll z tile gridu pod hero. Eyebrow + title +
   lede + CTAs do detail page na jedné straně, gallery na druhé. Sudé řady mají
   gallery vlevo přes :nth-of-type(even) flip. Okolí + Doprava merged do jedné
   sekce s 2 CTAs. */
type GalleryItem =
  | { src: string; alt: string }
  | { placeholder: string; alt: string };
type SectionCta = { href: string; label: string; aria?: string };
type Section = {
  id: string;
  eyebrow: string;
  title: string;
  lede: string;
  ctas: SectionCta[];
  gallery: GalleryItem[];
  /* Bezier d-form pro section ribbon pozadí, viewBox 0 0 1600 600.
     Sudé sekce dostanou scaleX(-1) takže path se zrcadlí, navrhuj path
     s důrazem v levé spodní třetině (negativní prostor pod textem). */
  ribbonPath: string;
};

const sections: Section[] = [
  {
    id: 'ubytovani',
    eyebrow: 'Ubytování',
    title: 'Privátní apartmán, nebo sdílený pokoj?',
    lede: 'Šest formátů sdílených pokojů od otevřeného lůžka po uzamykatelnou kapsli a pět privátních apartmánů 1+kk až 5+kk. V ceně energie, internet, coworkingový sál a pět jízd kyvadlové dopravy MyShelter k metru měsíčně.',
    ctas: [
      { href: '/ubytovani/', label: 'Zjistit více', aria: 'Zjistit více o ubytování' },
    ],
    ribbonPath: 'M -80 480 C 120 320, 320 240, 540 360 S 940 520, 1120 380 S 1400 280, 1560 360',
    gallery: [
      { src: withBase('/images/hub/ubytovani/01-kapsle.jpg'), alt: 'Uzamykatelná kapsle, dřevěné obložení a tlumené ambientní světlo.' },
      { src: withBase('/images/hub/ubytovani/02-1kk.jpg'), alt: 'Privátní apartmán 1+kk, postel u okna, kuchyňský kout a jídelní stůl.' },
      { src: withBase('/images/hub/ubytovani/03-ext.jpg'), alt: 'Privátní apartmány v parteru za soumraku, alej bříz, teplá světla v oknech.' },
      { src: withBase('/images/hub/ubytovani/04-int.jpg'), alt: 'Interiér privátního apartmánu, večerní atmosféra s teplým světlem.' },
      { src: withBase('/images/hub/ubytovani/05-4kk-family.jpg'), alt: 'Privátní apartmán 4+kk: rodina v obývacím pokoji s otevřenou kuchyní, dlouhý jídelní stůl pro osm.' },
    ],
  },
  {
    id: 'zazemi',
    eyebrow: 'Zázemí',
    title: 'Coworking, restaurace, wellness, bazén.',
    lede: 'Coworkingový sál s dílnami a zasedačkami, kantýna, pekárna a restaurace, venkovní bazén a sauny. Většina zázemí je pro ubytované v ceně nebo za zvýhodněnou sazbu, část otevřená i návštěvníkům.',
    ctas: [{ href: '/zazemi/', label: 'Zjistit více', aria: 'Zjistit více o zázemí' }],
    ribbonPath: 'M -60 380 C 180 460, 360 220, 580 380 S 960 540, 1140 360 S 1380 220, 1560 360',
    gallery: [
      { src: withBase('/images/hub/coworking/cowork-interier-sal-c.jpg'), alt: 'Coworkingový sál v budově C s pódiem a kuchyňským koutem.' },
      { src: withBase('/images/hub/komunita/gastro/01-kantyna.jpg'), alt: 'Kantýna a restaurace v kuchyňském bloku B, dřevěné stoly.' },
      { src: withBase('/images/hub/komunita/wellness-02-sauna.jpg'), alt: 'Sauna ve wellness objektu, dřevěné obložení a tlumené světlo.' },
      { src: withBase('/images/hub/komunita/01-bazen.jpg'), alt: 'Venkovní bazén v jádře areálu, sluneční terasa, relax po práci.' },
    ],
  },
  {
    id: 'doprava',
    eyebrow: 'Doprava',
    title: '15 minut na metro.',
    lede: 'Kyvadlová doprava MyShelter k metru Kobylisy a k přívozu Klecánky, MHD linka 374 minutu od areálu, půjčovna elektrovozidel a sdílené taxi. Pět jízd kyvadlovky a jednu jízdu taxi máte měsíčně v ceně.',
    ctas: [{ href: '/doprava/', label: 'Zjistit více', aria: 'Zjistit více o dopravě' }],
    ribbonPath: 'M -80 460 C 180 300, 380 460, 600 340 S 980 460, 1160 380 S 1380 280, 1560 380',
    gallery: [
      { src: withBase('/images/hub/doprava/hub-shuttle-zastavka.jpg'), alt: 'Zastávka kyvadlové dopravy MyShelter k metru Kobylisy.' },
      { src: withBase('/images/hub/okoli/01-areal-dostupnost.jpg'), alt: 'Mapa dostupnosti areálu, vzdálenosti na metro a do centra Prahy.' },
      { src: withBase('/images/hub/doprava/cyklostezka.jpg'), alt: 'Cyklostezka EuroVelo 7 podél Vltavy z Prahy do Klecan.' },
      { src: withBase('/images/hub/okoli/04-topview-update.jpg'), alt: 'Letecký pohled na areál Horních kasáren a obec Klecany.' },
    ],
  },
  {
    id: 'stipendia',
    eyebrow: 'Stipendia',
    title: 'Bydlení a tvorba za přínos komunitě.',
    lede: 'Pobytové stipendium pro lidi s projektem a projektové stipendium na materiál a dílenský čas. Bydlení nebo zvýhodněná cena za konkrétní vstup do života Hubu. Při registraci zaškrtnete zájem a ozveme se s podmínkami.',
    ctas: [{ href: '/stipendia/', label: 'Zjistit více', aria: 'Zjistit více o stipendiích' }],
    ribbonPath: 'M -60 420 C 160 540, 380 260, 600 400 S 960 520, 1140 380 S 1380 240, 1560 380',
    gallery: [
      { src: withBase('/images/hub/stipendia/01-atelier.jpg'), alt: 'Ateliér s rezidentem-tvůrcem v práci, denní světlo, velký stůl.' },
      { src: withBase('/images/hub/stipendia/03-rezidenti.jpg'), alt: 'Skupina rezidentů u společného stolu, plánování nebo večeře.' },
    ],
  },
];

/* Dvě karty pod úvodem sekce Ubytování, dva sloupce vedle sebe: scroll
   galerie (Gallery) nahoře, pod ní rámované tělo s nadpisem, popisem a
   tlačítkem Zjistit více. Vlevo Privátní apartmány, vpravo Sdílené pokoje.
   Velká slide gallery sekce je u Ubytování skrytá (úvod je na celou šířku). */
const ubytovaniCards = [
  {
    title: 'Privátní apartmány',
    text: 'Celý byt jen pro vás, od 1+kk po 5+kk pro skupinu až patnácti. Vlastní koupelna, kuchyň i vchod.',
    href: '/ubytovani/privatni/',
    aria: 'Zjistit více o privátních apartmánech',
    gallery: [
      { src: withBase('/images/hub/ubytovani/02-1kk.jpg'), alt: 'Privátní apartmán 1+kk, sklápěcí postel, kuchyňský kout a pracovní stůl.' },
      { src: withBase('/images/hub/ubytovani/privatni/2kk-01.jpg'), alt: 'Privátní apartmán 2+kk, obývací pokoj s otevřenou kuchyní.' },
      { src: withBase('/images/hub/ubytovani/privatni/3kk-01.jpg'), alt: 'Privátní apartmán 3+kk, prostorný obytný prostor pro skupinu.' },
      { src: withBase('/images/hub/ubytovani/05-4kk-family.jpg'), alt: 'Privátní apartmán 4+kk: rodina v obývacím pokoji, dlouhý jídelní stůl.' },
      { src: withBase('/images/hub/ubytovani/privatni/5kk-01.jpg'), alt: 'Privátní apartmán 5+kk pro větší skupinu.' },
    ],
  },
  {
    title: 'Sdílené pokoje',
    text: 'Lůžko nebo kapsle, ne celý byt. Šest formátů ve třech stupních soukromí, koupelnu a kuchyň sdílíte.',
    href: '/ubytovani/sdilene-pokoje/',
    aria: 'Zjistit více o sdílených pokojích',
    gallery: [
      { src: withBase('/images/hub/ubytovani/coliving/pokoj-basic-01.jpg'), alt: 'Sdílený pokoj se dvěma lůžky, pracovními stoly a světlým dubovým nábytkem.' },
      { src: withBase('/images/hub/ubytovani/coliving/pokoj-privacy-01.jpg'), alt: 'Lůžkové boxy s posuvnými dveřmi, zvýšená míra soukromí.' },
      { src: withBase('/images/hub/ubytovani/coliving/dvouluzko-01.jpg'), alt: 'Dvojlůžko ve sdíleném pokoji se zvýšenou mírou soukromí.' },
      { src: withBase('/images/hub/ubytovani/coliving/kapsle-single-01.jpg'), alt: 'Privátní uzamykatelná kapsle, dřevěné obložení a tlumené světlo.' },
      { src: withBase('/images/hub/ubytovani/coliving/kapsle-double-01.jpg'), alt: 'Kapslové dvojlůžko s vysokou mírou soukromí.' },
    ],
  },
];

/* === RIBBON TILES (pod hero) ===
   Pět scroll dlaždic + Rezervace tile (6 celkem, 3×2 grid). Vizuální jazyk
   sjednocen s CrossLinkPair komponentou: 4 varianty (deep, cream, rose, ink)
   rotují tak aby žádné dva sousedy neměly stejnou. Eyebrow (uppercase small caps)
   + bold title, vpravo dole 36×36 kruhová šipka. Ribbon arc zůstává v pozadí. */
/* Tile variant zigzag: 1, 3, 5 cream + 2, 4, 6 ink. Stejný ribbon stroke
   napříč ink tiles, stejný ribbon stroke napříč cream tiles. */
/* Titulky mají vynucené zalomení (<br>) do PŘESNĚ 2 řádků, aby 3×2 grid
   vypadal uniformně i na širokém desktopu (přirozené zalomení by tam
   krátké popisky stáhlo na 1 řádek). Font scaluje s šířkou dlaždice (vw),
   takže i nejdelší řádek („Pobytová & projektová") padne na jeden řádek
   napříč celým desktop rozsahem. */
const tiles = [
  { icon: 'bed-double',     title: 'Coliving<br>&amp; bydlení',              href: '#ubytovani', variant: 'cream', dy: '26%',
    path: 'M 60 240 C 220 100, 380 320, 540 200 C 660 100, 720 260, 760 340' },
  { icon: 'award',          title: 'Pobytová &amp; projektová<br>stipendia', href: '#stipendia', variant: 'ink', dy: '26%',
    path: 'M 60 240 C 200 100, 380 320, 520 200 C 660 80, 740 240, 760 360' },
  { icon: 'concierge-bell', title: 'Zázemí<br>&amp; coworking',             href: '#zazemi',    variant: 'cream', dy: '26%',
    path: 'M 80 120 C 240 280, 400 80, 560 240 C 680 360, 740 200, 760 320' },
  { icon: 'bus',            title: 'Dopravní<br>dostupnost',                href: '#doprava',   variant: 'ink', dy: '26%',
    path: 'M 80 200 C 240 80, 420 280, 580 160 C 700 80, 740 240, 760 320' },
  { icon: 'images',         title: 'Fotogalerie<br>areálu',                 href: '#galerie',   variant: 'cream', dy: '14%',
    path: 'M 80 200 C 200 360, 420 80, 580 240 C 700 360, 740 200, 760 100' },
];
---
<Base
  title="Startovací Hub Klecany · Bydlení a coworking 15 min od Prahy"
  description="Jedenáct formátů bydlení od sdíleného lůžka po byt 5+kk, coworking, dílny, sauna a kavárna. Horní kasárna Klecany, patnáct minut autobusem od metra Kobylisy. Od 3 000 Kč měsíčně, energie a internet v ceně."
  headerVariant="overlay">

  {/* ============ HERO ROTATION ============ */}
  <section class="lp-hero" data-hero-mode="video" aria-label="Startovací Hub Klecany, hero galerie">
    <div class="lp-hero__rotation" aria-hidden="true">
      {heroSlides.map((slide, i) => (
        <div class="lp-hero__slide" data-slide={i} style={`animation-delay: ${i * 5 - 1}s`}>
          <picture>
            <source media="(max-width: 768px)" srcset={slide.phone} />
            <img class="lp-hero__bg" src={slide.desktop} alt={slide.alt} loading={i === 0 ? 'eager' : 'lazy'} decoding="async" fetchpriority={i === 0 ? 'high' : undefined} />
          </picture>
        </div>
      ))}
    </div>
    {/* Video varianta: skrytá defaultně, JS přepíná na data-hero-mode="video".
        Browser load only on demand (preload="none" + lazy src set via JS) aby
        prefence „slides" měla žádný bandwidth overhead. */}
    <video
      class="lp-hero__video"
      data-hero-video
      data-src-desktop={withBase('/videos/hero-reel-1.mov')}
      data-src-phone={withBase('/videos/hero-reel-1-phone.mov')}
      muted
      loop
      playsinline
      preload="auto"
      aria-hidden="true"></video>
    <div class="lp-hero__scrim" aria-hidden="true"></div>

    <div class="lp-hero__inner">
      <h1 class="lp-hero__title">
        <BrandLogo class="lp-hero__logo" />
        <span class="sr-only">Startovací Hub</span>
      </h1>
      <p class="lp-hero__lede">your perfect living &amp; working space</p>
    </div>

    {/* Dvě rychlá CTA do rezervačního formuláře s předvyplněnou cestou, spojená
        do segmentového skleněného pruhu. Desktop: centrovaná dvojice u spodního
        okraje hero. Mobil: in-flow sloupec pod textem. Pod každým tlačítkem
        drobný popisek formátu. */}
    <div class="lp-hero__quick" role="group" aria-label="Rychlý pronájem">
      <a class="lp-hero__quick-btn osa-nolink" href={withBase('/rezervace/?cesta=apartman')}>
        <span class="lp-hero__quick-label">Pronajmout apartmán</span>
        <span class="lp-hero__quick-icon" aria-hidden="true"><Arrow dir="right" variant="chevron" size={18} weight={2.6} /></span>
      </a>
      <a class="lp-hero__quick-btn osa-nolink" href={withBase('/rezervace/?cesta=luzko')}>
        <span class="lp-hero__quick-label">Pronajmout lůžko</span>
        <span class="lp-hero__quick-icon" aria-hidden="true"><Arrow dir="right" variant="chevron" size={18} weight={2.6} /></span>
      </a>
    </div>

    {/* Scroll cue: kruhový button s chevron-down, jemně poskakuje + pulsing ring,
        klik smooth-scrolluje na jump strip pod hero. */}
    <a class="lp-hero__scroll-cue osa-nolink" href="#lp-jumps" aria-label="Skrolovat na možnosti">
      <Icon name="chevron-down" size={24} strokeWidth={2} />
    </a>
  </section>

  <script is:inline>
    /* Hero video: default loop reel. Uživatelé s prefers-reduced-motion: reduce
       dostanou statické slides (bez autoplaying motion). Žádný toggle, žádný
       localStorage. Video src se nastaví lazily podle viewportu. */
    (function () {
      if (window.__hubHeroVideoBound) return;
      window.__hubHeroVideoBound = true;
      function bind() {
        var hero = document.querySelector('.lp-hero');
        var video = document.querySelector('[data-hero-video]');
        if (!hero || !video) return;
        if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
          hero.dataset.heroMode = 'slides';
          return;
        }
        hero.dataset.heroMode = 'video';
        if (!video.src) {
          var isPhone = window.matchMedia('(max-width: 768px)').matches;
          video.src = isPhone ? video.dataset.srcPhone : video.dataset.srcDesktop;
        }
        video.play().catch(function () {});
      }
      document.addEventListener('astro:page-load', bind);
    }());
  </script>

  {/* Tile grid POD hero (3×2). Pět scroll-anchor dlaždic + Rezervace tile
      jako šestá. Každá dlaždice má velký ribbon arc jako pozadí (peeká z
      pod typo), per spec § 04 banner influence (plakátovější vibe). */}
  {/* DOČASNĚ SKRYTO 2026-06-18 (přání klienta): celý rozcestník 6 dlaždic pod
      hero je zakomentovaný kvůli snadnému obnovení. Odkomentovat = smazat tento
      úvodní JSX-komentář a jeho uzavírací značku umístěnou hned za </section>.
  <section class="lp-tiles" id="lp-jumps" aria-label="Skok do sekcí">
    <div class="lp-tiles__grid">
      {tiles.map((t) => (
        <a class={`lp-tile lp-tile--${t.variant} osa-nolink`} href={t.href}>
          <span class="lp-tile__ribbon" aria-hidden="true" data-ribbon-reveal style={`--lp-ribbon-dy:${t.dy}`}>
            <svg viewBox="0 0 800 600" preserveAspectRatio="xMidYMid slice">
              <path d={t.path} />
            </svg>
          </span>
          <span class="lp-tile__type">
            <span class="lp-tile__icon" aria-hidden="true"><Icon name={t.icon} size={22} strokeWidth={1.9} /></span>
            <span class="lp-tile__title" set:html={t.title} />
          </span>
          <span class="lp-tile__arrow" aria-hidden="true">
            <Arrow dir="right" variant="chevron" size={18} weight={2.2} />
          </span>
        </a>
      ))}

      <a class="lp-tile lp-tile--reserve osa-nolink" href={withBase('/rezervace/')}>
        <span class="lp-tile__ribbon" aria-hidden="true" data-ribbon-reveal style="--lp-ribbon-dy:14%">
          <svg viewBox="0 0 800 600" preserveAspectRatio="xMidYMid slice">
            <path d="M 540 80 C 720 100, 760 240, 600 320 C 460 400, 380 280, 540 200" />
          </svg>
        </span>
        <span class="lp-tile__type">
          <span class="lp-tile__icon" aria-hidden="true"><Icon name="calendar-check" size={22} strokeWidth={1.9} /></span>
          <span class="lp-tile__title">Nezávazná<br>rezervace</span>
        </span>
        <span class="lp-tile__arrow lp-tile__arrow--filled" aria-hidden="true">
          <Arrow dir="right" variant="chevron" size={18} weight={2.4} />
        </span>
      </a>
    </div>
  </section>
  */}

  {/* ============ ZIG-ZAG SEKCE (5 řad, one-pager) ============ */}
  <div class="lp-sections">
    {sections.map((sec) => (
      <>
        <section class={`lp-section${sec.id === 'ubytovani' ? ' lp-section--intro' : ''}`} id={sec.id} aria-labelledby={`${sec.id}-title`}>
          <span class="lp-section__ribbon" aria-hidden="true" data-ribbon-reveal>
            <svg viewBox="0 0 1600 600" preserveAspectRatio="xMidYMid slice">
              <path d={sec.ribbonPath} vector-effect="non-scaling-stroke" />
            </svg>
          </span>
          <div class="lp-section__inner">
            <div class="lp-section__text" data-reveal>
              {/* eyebrow dočasně skryt (klient 2026-06-18): <p class="lp-section__eyebrow">{sec.eyebrow}</p> */}
              <h2 class="lp-section__title" id={`${sec.id}-title`}>{sec.title}</h2>
              <p class="lp-section__lede">{sec.lede}</p>
              {sec.ctas.length > 0 && (
                <div class="lp-section__ctas">
                  {sec.ctas.map((cta) => (
                    <a class="lp-section__cta osa-nolink" href={withBase(cta.href)} aria-label={cta.aria}>
                      <span class="lp-section__cta-label">{cta.label}</span>
                      <span class="lp-section__cta-arrow" aria-hidden="true"><Arrow dir="right" variant="chevron" size={16} weight={2.4} /></span>
                    </a>
                  ))}
                </div>
              )}
            </div>
            {sec.id !== 'ubytovani' && (
              <div class="lp-section__media" data-reveal data-reveal-delay="1">
                <Gallery images={sec.gallery} ariaLabel={sec.eyebrow} aspectRatio="4 / 3" />
              </div>
            )}
          </div>

          {/* DOČASNĚ SKRYTO 2026-06-18 (přání klienta): pruh „Vždy v ceně bydlení"
              (ResidentBenefits, benefit chipy s ikonami a textem, co je zahrnuto
              v ceně bydlení). Ponecháno zakomentované pro pozdější obnovení.
          {sec.id === 'ubytovani' && (
            <div class="lp-section__benefits">
              <ResidentBenefits eyebrow={false} />
            </div>
          )}
          */}

        {/* Dvě karty UVNITŘ sekce Ubytování (ať ribbon i pozadí přecházejí přes
            ně, žádná dělící čára mezi úvodem a kartami), dva sloupce v rovině
            pod úvodním textem: scroll galerie nahoře, pod ní rámované tělo
            s nadpisem, popisem a tlačítkem Zjistit více. */}
        {sec.id === 'ubytovani' && (
          <div class="lp-ucards">
            {ubytovaniCards.map((card) => (
              <article class="lp-ucard" data-reveal>
                <div class="lp-ucard__media">
                  <Gallery images={card.gallery} ariaLabel={card.title} aspectRatio="4 / 3" />
                </div>
                <div class="lp-ucard__body">
                  <h3 class="lp-ucard__title"><a class="lp-ucard__title-link osa-nolink" href={withBase(card.href)}>{card.title}</a></h3>
                  <p class="lp-ucard__text">{card.text}</p>
                  <a class="lp-ucard__cta osa-nolink" href={withBase(card.href)} aria-label={card.aria}>
                    <span class="lp-ucard__cta-label">Zjistit více</span>
                    <span class="lp-ucard__cta-arrow" aria-hidden="true"><Arrow dir="right" variant="chevron" size={16} weight={2.4} /></span>
                  </a>
                </div>
              </article>
            ))}
          </div>
        )}

        </section>
      </>
    ))}
  </div>

  {/* Reklamní cross-link karty pod galerií , prolink do produktu (ne rezervace).
      Finální CTA banner odstraněn 16. 6. 2026, rezervaci řeší pruh v patičce. */}
  {/* DOČASNĚ SKRYTO 2026-06-18 (přání klienta): 2 cross-link karty na konci landingu před patičkou.
  <CrossLinkPair
    sectionLabel="Pokračujte na ubytování a zázemí"
    items={[
      { eyebrow: 'Ubytování', title: 'Apartmány, pokoje a kapsle', href: '/ubytovani/', variant: 'cream',
        ribbonPath: 'M 600 360 C 780 250, 1000 420, 1180 300 C 1340 200, 1460 360, 1540 280' },
      { eyebrow: 'Zázemí', title: 'Coworking, restaurace, wellness', href: '/zazemi/', variant: 'rose', ribbonWidth: 30,
        ribbonPath: 'M 140 380 C 420 220, 700 460, 980 320 C 1220 200, 1420 420, 1560 300' },
    ]}
  />
  */}
</Base>

<style>
  /* ============ HERO ROTATION ============ */
  /* 4 slides absolutely overlapped, 20 s cyklus, 5 s na slide,
     swipe 1 s. animation-delay posunuje každý slide v cyklu. */
  .lp-hero {
    position: relative;
    isolation: isolate;
    /* Full-screen na desktop i mobile. dvh = dynamic viewport height,
       řeší iOS Safari URL bar bez jumping při scrollu. Fallback vh pro
       starší prohlížeče. Header je position: fixed (overlay variant),
       takže hero může jít plně pod něj. Horizontální gutter je na
       __inner aby content alignoval s header bar pattern (max-width +
       padding-x + margin auto).
       Background image fallback (h00 desktop): vyplní hero okamžitě
       z CSS než se IMG slide dolaďí, žádný plum-deep flash při cold
       reload. Slide carousel se layeruje navrch přes z-index -2. */
    min-height: 100vh;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: var(--space-9) 0;
    color: #fff;
    background-color: var(--plum-deep);
    background-image: url('/sh-web/images/hub/hero/h00.jpg');
    background-size: cover;
    background-position: center;
    overflow: hidden;
  }
  @media (max-width: 768px) {
    .lp-hero { background-image: url('/sh-web/images/hub/hero/h00-phone.jpg'); }
  }
  .lp-hero__rotation {
    position: absolute;
    inset: 0;
    z-index: -2;
  }
  /* Video varianta: skrytá defaultně. Přepnutí přes data-hero-mode="video"
     na .lp-hero. Video pokrývá celou plochu (object-fit cover) jako slides.
     Slides rotation skryta, video viditelné. */
  .lp-hero__video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -2;
    display: none;
    pointer-events: none;
    filter: brightness(0.87);
  }
  .lp-hero[data-hero-mode="video"] .lp-hero__rotation { display: none; }
  .lp-hero[data-hero-mode="video"] .lp-hero__video { display: block; }
  /* Video režim překryje i CSS background-image fallback (h00.jpg). */
  .lp-hero[data-hero-mode="video"] {
    background-image: none;
  }
  /* Dvě rychlá CTA jako liquid-glass pilulky (jen tlačítka, bez popisků).
     Hover = jednoduché čisté zvýraznění (pilulka se projasní na téměř bílou,
     text zfialoví, šipka se posune). Žádný glow, žádné smyčkové animace.
     Skupina ukotvená u spodní hrany hero, hned nad scroll šipkou; logo + claim
     zůstávají vertikálně na středu (jediný in-flow obsah).
     Mobil/tablet: stack na střed. Desktop (níž): dva poloviční sloupce dělené
     PŘESNĚ ve středu stránky, mezera (a tím i scroll šipka) je vždy na středu,
     pilulky mají šířku dle obsahu (celek může být lehce offsetnutý). */
  .lp-hero__quick {
    --rgap: clamp(20px, 3vw, 44px);
    position: absolute;
    left: 0;
    right: 0;
    bottom: clamp(92px, 12vh, 128px);
    z-index: 1;
    display: grid;
    justify-items: center;
    row-gap: 14px;
    padding: 0 var(--gutter);
  }
  .lp-hero__quick-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 14px;
    width: 100%;
    max-width: 340px;
    padding: 18px 34px;
    border-radius: 999px;
    color: #fff;
    text-decoration: none;
    font: 700 var(--text-lg) / 1 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    border: 1px solid rgba(255, 255, 255, 0.32);
    background: linear-gradient(150deg, rgba(255,255,255,0.20), rgba(255,255,255,0.06) 50%, rgba(255,255,255,0.10));
    backdrop-filter: blur(16px) saturate(160%);
    -webkit-backdrop-filter: blur(16px) saturate(160%);
    box-shadow: 0 1px 0 0 rgba(255,255,255,0.45) inset, 0 12px 28px -14px rgba(0,0,0,0.55);
    transition: background 200ms var(--ease-quart),
      border-color 200ms var(--ease-quart),
      color 200ms var(--ease-quart),
      transform 200ms var(--ease-quart);
  }
  .lp-hero__quick-label { white-space: nowrap; }
  .lp-hero__quick-icon {
    display: inline-flex;
    align-items: center;
    flex-shrink: 0;
    transition: transform 200ms var(--ease-quart);
  }
  .lp-hero__quick-btn:hover,
  .lp-hero__quick-btn:focus-visible {
    background: rgba(255, 255, 255, 0.94);
    border-color: #fff;
    color: var(--plum-deep);
    transform: translateY(-2px);
    outline: none;
  }
  .lp-hero__quick-btn:focus-visible { outline: 2px solid #fff; outline-offset: 3px; }
  .lp-hero__quick-btn:hover .lp-hero__quick-icon,
  .lp-hero__quick-btn:focus-visible .lp-hero__quick-icon { transform: translateX(4px); }
  /* Desktop: centrovaná dvojice tlačítek u spodní hrany hero, hned nad scroll
     šipkou. Šířka dle obsahu, mezi nimi sevřená mezera. */
  /* Desktop: vše centrované v jednom sloupci (logo , podtitul , velká tlačítka).
     Tlačítka zůstávají v toku pod podtitulem, celá skupina je svisle na střed. */
  @media (min-width: 769px) {
    .lp-hero__quick {
      --rgap: clamp(28px, 4vw, 56px);
      grid-template-columns: 1fr 1fr;
      align-items: center;
      row-gap: 0;
    }
    /* Pilulky šířka dle obsahu, zarovnané ke středu (levá zprava, pravá zleva),
       mezera --rgap je vždy na středu stránky. */
    .lp-hero__quick-btn { width: auto; max-width: none; padding: 22px 48px; font-size: clamp(1.275rem, 1.44vw, 1.5rem); }
    .lp-hero__quick-btn:nth-child(1) { justify-self: end; margin-right: calc(var(--rgap) / 2); }
    .lp-hero__quick-btn:nth-child(2) { justify-self: start; margin-left: calc(var(--rgap) / 2); }
  }
  @media (max-width: 540px) {
    .lp-hero__quick { row-gap: 16px; }
  }
  /* Telefon na šířku (nízký viewport): hero content se nevejde do 100dvh,
     pokud CTA pruh visí absolutně u spodní hrany přes střed s titulkem.
     Vrátíme pilulky do toku pod lede, schováme scroll-cue (není kam scrollovat
     gestem na pár set px) a necháme hero přirozeně narůst + odscrollovat. */
  @media (orientation: landscape) and (max-height: 600px) {
    .lp-hero {
      min-height: 100dvh;
      justify-content: center;
      padding: calc(68px + var(--space-4)) 0 var(--space-5);
    }
    .lp-hero__inner { transform: none; }
    .lp-hero__quick {
      position: static;
      left: auto;
      right: auto;
      bottom: auto;
      margin: var(--space-4) auto 0;
      --rgap: 16px;
    }
    .lp-hero__quick-btn { padding: 12px 24px; font-size: var(--text-md); }
  }
  /* 5 slidů jako horizontal carousel: 4 s nehybný hold, pak 1 s swipe
     celé fotky doleva. Sousední slide zároveň zprava vplouvá dovnitř,
     takže přechod je jeden plynulý sweep bez cross-fade.
     Slide N animation-delay = N × 5 s. Slide 0 (h00) má delay 0,
     swipne-in jako první při page loadu (krátká 1 s brand-deep
     opona předtím než h00 vplouvá zprava).
     Smyčka 25 s (5 × 5 s). Keyframes: swipe-in 4 %, hold 4 až 20 %,
     swipe-out 20 až 24 %. */
  .lp-hero__slide {
    position: absolute;
    inset: 0;
    transform: translateX(100%);
    animation: lp-hero-swipe 25s cubic-bezier(0.65, 0, 0.35, 1) infinite;
    will-change: transform;
  }

  @keyframes lp-hero-swipe {
    0%   { transform: translateX(100%); }
    4%   { transform: translateX(0); }
    20%  { transform: translateX(0); }
    24%  { transform: translateX(-100%); }
    100% { transform: translateX(-100%); }
  }

  /* Brightness 0.87 (~13 % ztmavení) , hero video/fotka o kus tmavší pro
     atmosféru a čitelnost bílého copy. Scrim ztlumený v dolních pixelech
     (60→30 %) ať atmosféra fotky proteče. */
  .lp-hero__bg {
    position: absolute;
    inset: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    filter: brightness(0.87);
  }
  .lp-hero__scrim {
    position: absolute;
    inset: 0;
    /* Dvě vrstvy: top scrim pro čitelnost menu nad video bg + bottom scrim
       pro čitelnost hero title/lede. Stack: top gradient zhasne na 35 %,
       bottom gradient nabíhá od 70 %, mezi nimi čistá fotka/video. */
    background:
      linear-gradient(
        to bottom,
        rgba(0,0,0,0.4) 0%,
        rgba(0,0,0,0.18) 18%,
        rgba(0,0,0,0) 35%
      ),
      linear-gradient(
        to top,
        rgba(0,0,0,0.80) 0%,
        rgba(0,0,0,0.58) 22%,
        rgba(0,0,0,0.28) 48%,
        rgba(0,0,0,0) 78%
      );
    z-index: -1;
  }
  .lp-hero__inner {
    /* Width pattern: matches tile grid edge (1480 max, gutter-aware). */
    width: calc(100% - 2 * var(--gutter));
    max-width: var(--container-wide);
    margin: 0 auto;
    text-align: center;
  }
  .lp-hero__title {
    /* Menší mezera pod logem , lede „your perfect..." sedí blíž k logu. */
    margin: 0 0 var(--space-1);
    line-height: 0;
  }
  :global(.lp-hero__logo) {
    /* Velký centrovaný wordmark. Výška laditelná za běhu. Zmenšeno ~20 %, pak
       o dalších 8 % (2026-06-26), aby šířka loga seděla na šířku lede textu. */
    display: block;
    margin: 0 auto;
    height: clamp(1.91rem, 3.39vw, 3.31rem);
    width: auto;
    max-width: 100%;
    color: #fff;
  }
  .lp-hero__lede {
    /* Podnadpis zvětšen ~20 % (2026-06-26). */
    font: 500 clamp(1.2rem, 1.56vw, 1.55rem) / 1.4 var(--font-sans);
    letter-spacing: var(--tracking-wide);
    margin: 0 auto;
    color: rgba(255,255,255,0.92);
    max-width: 44ch;
  }

  /* === SCROLL CUE === */
  /* Kruhový button dole na hero, který smooth-scrolluje na jump strip.
     Glass-morph styling (blur + transparent), uvnitř bouncing chevron,
     kolem subtle pulsing ring jako delight cue. */
  .lp-hero__scroll-cue {
    position: absolute;
    bottom: clamp(20px, 4vh, 36px);
    left: 50%;
    transform: translateX(-50%);
    width: 48px;
    height: 48px;
    border-radius: 999px;
    border: 1.5px solid rgba(255, 255, 255, 0.55);
    background: rgba(0, 0, 0, 0.22);
    color: #fff;
    display: grid;
    place-items: center;
    text-decoration: none;
    cursor: pointer;
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    transition: border-color 220ms ease, background 220ms ease, transform 220ms cubic-bezier(0.2, 0, 0, 1);
    z-index: 1;
  }
  .lp-hero__scroll-cue::before {
    /* 4. beat sekvence: pulse ring + bounce po třech tlačítkách. */
    content: '';
    position: absolute;
    inset: -6px;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.45);
    opacity: 0;
    animation: lp-seq-cue-ping 8s var(--ease-quart) infinite 6s;
    pointer-events: none;
  }
  .lp-hero__scroll-cue :global(svg) {
    display: block;
    animation: lp-seq-cue-bounce 8s var(--ease-quart) infinite 6s;
  }
  .lp-hero__scroll-cue:hover,
  .lp-hero__scroll-cue:focus-visible {
    border-color: #fff;
    background: rgba(255, 255, 255, 0.18);
    transform: translateX(-50%) translateY(2px);
    outline: none;
  }
  .lp-hero__scroll-cue:hover :global(svg),
  .lp-hero__scroll-cue:focus-visible :global(svg) { animation: none; }
  .lp-hero__scroll-cue:hover::before,
  .lp-hero__scroll-cue:focus-visible::before { animation: none; opacity: 0; }
  @keyframes lp-seq-cue-bounce {
    0%, 4%, 24%, 100% { transform: translateY(0); }
    13% { transform: translateY(5px); }
  }
  @keyframes lp-seq-cue-ping {
    0%, 100% { opacity: 0; transform: scale(1); }
    3%  { opacity: 0.7; transform: scale(1); }
    22% { opacity: 0; transform: scale(1.5); }
  }

  /* reduced-motion: freeze on slide 0, vypnout cue + sekvenční animace */
  @media (prefers-reduced-motion: reduce) {
    .lp-hero__slide { animation: none; transform: translateX(-100%); }
    .lp-hero__slide[data-slide="0"] { transform: translateX(0); }
    .lp-hero__scroll-cue :global(svg),
    .lp-hero__scroll-cue::before { animation: none; }
    .lp-hero__scroll-cue::before { opacity: 0; }
    .lp-hero__quick-icon { transition: none; }
  }

  /* ============ TILE GRID (3×2, pod hero) ============
     Pět scroll-anchor dlaždic + Rezervace tile jako šestá. Velký ribbon arc
     na pozadí (z-index 0, peeká z pod typo), typo zaplňuje plochu. Variant
     fill drží light na landingu (cream/soft) protože foto v sekcích níže
     potřebují bílé pozadí, výraznější colorize jde do subpage hero.
     Per spec § 04 banner influence, plakátový styl bez čísel. */
  .lp-tiles {
    background: var(--bg);
    padding: var(--space-6) 0 var(--space-7);
    border-bottom: 1px solid var(--border-muted);
    scroll-margin-top: 80px;
  }
  /* Mobile: ribbon wave nad první sekcí dělá visual break sám,
   * 1px linka pod ní vypadá redundantně. Desktop ji drží jako jemný rule. */
  @media (max-width: 540px) {
    .lp-tiles { border-bottom: 0; }
  }
  .lp-tiles__grid {
    /* Stejný width pattern jako zbytek obsahu webu (header bar, lp-section,
       footer): container-wide se 2× gutter padding. Tile karty mírně užší
       díky větší mezeře v gridu. */
    width: calc(100% - 2 * var(--gutter));
    max-width: var(--container-wide);
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: clamp(16px, 2.4vw, 32px);
  }

  /* Tile, mirror cross-link card: 1fr auto grid (text left, arrow circle right),
     align-items end, min-height clamp, padding clamp, border-radius input, ribbon
     arc v pozadí z-index 0, isolation isolate pro bezpečný stacking context. */
  .lp-tile {
    position: relative;
    display: grid;
    grid-template-columns: 1fr auto;
    gap: clamp(16px, 2vw, 24px);
    align-items: end;
    min-height: clamp(104px, 12vw, 150px);
    padding: clamp(14px, 1.7vw, 20px) clamp(20px, 2.2vw, 28px);
    border-radius: var(--radius-input);
    text-decoration: none !important;
    overflow: hidden;
    isolation: isolate;
    transition:
      transform 320ms var(--ease-quart),
      box-shadow 320ms var(--ease-quart);
  }

  /* Varianty zrcadlí cross-link: deep (plum bg + bílý text), cream (kremový bg
     s tenkým accent borderem), rose (rose bg + plum text), ink (plum bg + cream
     text). Eyebrow color + ribbon stroke per varianta. */
  .lp-tile--deep  { background: var(--plum-deep); color: #fff; }
  .lp-tile--cream { background: var(--cream); color: var(--fg); border: 1px solid var(--accent-rule); }
  .lp-tile--rose  { background: var(--rose); color: var(--plum-deep); }
  .lp-tile--ink   { background: var(--plum-deep); color: var(--cream); }
  /* Rezervace tile: CTA varianta s gradient pozadím (plum-deep → rose tint),
     výraznější titulkem a pulse dot indikátorem. Vizuálně nejnápadnější
     ze všech 6 tilů, je to primární konverze celé landing. */
  .lp-tile--reserve {
    background:
      radial-gradient(circle at 90% 110%, hsl(calc(var(--hue) + 44) 30% 70% / 0.32) 0%, transparent 55%),
      linear-gradient(135deg, hsl(var(--hue) 41% 13%) 0%, hsl(var(--hue) 36% 22%) 100%);
    color: #fff;
    box-shadow: 0 6px 24px hsl(var(--hue) 41% 13% / 0.18);
  }
  .lp-tile--reserve .lp-tile__eyebrow { color: var(--rose); }
  /* Title font-size dědí ze .lp-tile__title (sjednocený s ostatními tily),
   * jen color override na bílou pro kontrast s plum-deep bg. */
  .lp-tile--reserve .lp-tile__title { color: #fff; }
  .lp-tile--reserve .lp-tile__ribbon path { stroke: var(--rose); stroke-width: 28; }
  /* Pulse dot, 8 px vlevo nahoře, jemný breathing pulse pro „live" cue. */
  .lp-tile__pulse {
    position: absolute;
    top: clamp(16px, 2vw, 22px);
    right: clamp(16px, 2vw, 22px);
    width: 10px;
    height: 10px;
    border-radius: 999px;
    background: var(--rose);
    z-index: 2;
    box-shadow: 0 0 0 0 hsl(calc(var(--hue) + 44) 30% 70% / 0.7);
    animation: lp-tile-pulse 2.4s ease-out infinite;
  }
  @keyframes lp-tile-pulse {
    0%   { box-shadow: 0 0 0 0 hsl(calc(var(--hue) + 44) 30% 70% / 0.6); }
    70%  { box-shadow: 0 0 0 12px hsl(calc(var(--hue) + 44) 30% 70% / 0); }
    100% { box-shadow: 0 0 0 0 hsl(calc(var(--hue) + 44) 30% 70% / 0); }
  }
  /* Arrow filled: na rezervace tile už defaultně vyplněný kruh rose s plum-deep
     ikonou. Hover: scale + slide right. */
  .lp-tile__arrow--filled {
    background: var(--rose);
    border-color: var(--rose);
    opacity: 1;
    color: var(--plum-deep);
  }
  .lp-tile__arrow--filled :global(svg) { color: var(--plum-deep); }
  .lp-tile--reserve:hover .lp-tile__arrow--filled,
  .lp-tile--reserve:focus-visible .lp-tile__arrow--filled {
    background: #fff;
    border-color: #fff;
    transform: translateX(4px) scale(1.05);
  }
  @media (prefers-reduced-motion: reduce) {
    .lp-tile__pulse { animation: none; }
  }

  /* Ikona + titul v jednom řádku, bottom-aligned (grid align-items end). */
  .lp-tile__type {
    position: relative;
    z-index: 2;
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 10px;
    max-width: 28ch;
    min-width: 0;
  }
  .lp-tile__icon { display: inline-flex; flex-shrink: 0; }
  .lp-tile__icon :global(svg) { display: block; }
  .lp-tile--cream .lp-tile__icon { color: var(--accent); }
  .lp-tile--rose .lp-tile__icon  { color: var(--plum-deep); }
  .lp-tile--ink .lp-tile__icon,
  .lp-tile--deep .lp-tile__icon,
  .lp-tile--reserve .lp-tile__icon { color: var(--rose); }
  .lp-tile__eyebrow {
    font: 500 var(--text-2xs) / 1 var(--font-sans);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
  }
  .lp-tile--deep .lp-tile__eyebrow  { color: var(--rose); }
  .lp-tile--cream .lp-tile__eyebrow { color: var(--accent); }
  .lp-tile--rose .lp-tile__eyebrow  { color: var(--plum-deep); }
  .lp-tile--ink .lp-tile__eyebrow   { color: var(--rose); }
  .lp-tile__title {
    /* Mírně větší než předtím; scaluje s šířkou dlaždice (vw), aby i nejdelší
       řádek („Pobytová & projektová") zůstal na jednom řádku napříč celým
       desktop rozsahem. Zalomení na 2 řádky řeší <br> v titulku. */
    font: 700 clamp(0.7rem, 1.35vw, 1.375rem) / 1.16 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    color: inherit;
    text-wrap: balance;
  }

  /* Arrow: 36×36 kruh, 1px border currentColor, opacity 0.7 idle.
     Na hover currentColor fill + svg flip na bg barvu karty. */
  .lp-tile__arrow {
    position: relative;
    z-index: 2;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 999px;
    border: 1px solid currentColor;
    opacity: 0.7;
    color: inherit;
    transition:
      opacity 220ms var(--ease-quart),
      transform 220ms var(--ease-quart),
      background 220ms var(--ease-quart);
  }
  .lp-tile__arrow :global(svg) { display: block; }

  /* Ribbon: background arc, position absolute inset 0, z-index 0, opacity 0.92,
     stejné snake-draw + breathing chování jako cross-link. */
  .lp-tile__ribbon {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.92;
    /* Posun tahu níž (vyplní pozadí pod ikonou/titulkem), per dlaždice. */
    transform: translateY(var(--lp-ribbon-dy, 0));
    transition: transform 700ms var(--ease-quart);
  }
  .lp-tile__ribbon svg {
    width: 100%;
    height: 100%;
    overflow: visible;
    display: block;
  }
  .lp-tile__ribbon path {
    fill: none;
    stroke-width: 32;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  /* Ribbon stroke per varianta, sjednoceno s cross-link. */
  .lp-tile--deep  .lp-tile__ribbon path { stroke: var(--accent); stroke-width: 38; }
  .lp-tile--cream .lp-tile__ribbon path { stroke: var(--accent); stroke-width: 32; }
  .lp-tile--rose  .lp-tile__ribbon path { stroke: var(--plum-deep); stroke-width: 12; }
  .lp-tile--ink   .lp-tile__ribbon path { stroke: var(--rose); stroke-width: 28; }

  .lp-tile__ribbon[data-ribbon-reveal] path {
    stroke-dasharray: 1;
    stroke-dashoffset: 1;
    pathLength: 1;
  }
  .lp-tile__ribbon[data-ribbon-reveal].is-revealed path {
    animation:
      ribbon-snake-draw 2.6s var(--ease-snake) forwards,
      ribbon-breathe 7s ease-in-out 2.6s infinite;
  }

  /* Hover, mirror cross-link: lift translateY -3px + plum shadow + arrow becomes
     filled (background = currentColor, svg color flips to tile bg). Ribbon slide
     14 px + pause breathing. */
  .lp-tile:hover,
  .lp-tile:focus-visible {
    transform: translateY(-3px);
    box-shadow: 0 18px 40px hsl(var(--hue) 41% 13% / 0.22);
    outline: none;
  }
  .lp-tile:hover .lp-tile__ribbon {
    transform: translate(14px, var(--lp-ribbon-dy, 0));
  }
  .lp-tile:hover .lp-tile__ribbon path {
    animation-play-state: paused;
    opacity: 0.85;
  }
  .lp-tile:hover .lp-tile__arrow,
  .lp-tile:focus-visible .lp-tile__arrow {
    opacity: 1;
    transform: translateX(3px);
    background: currentColor;
  }
  /* Šipka flip: svg color = bg color karty. */
  .lp-tile--deep:hover .lp-tile__arrow :global(svg),
  .lp-tile--deep:focus-visible .lp-tile__arrow :global(svg)  { color: var(--plum-deep); }
  .lp-tile--cream:hover .lp-tile__arrow :global(svg),
  .lp-tile--cream:focus-visible .lp-tile__arrow :global(svg) { color: var(--cream); }
  .lp-tile--rose:hover .lp-tile__arrow :global(svg),
  .lp-tile--rose:focus-visible .lp-tile__arrow :global(svg)  { color: var(--rose); }
  .lp-tile--ink:hover .lp-tile__arrow :global(svg),
  .lp-tile--ink:focus-visible .lp-tile__arrow :global(svg)   { color: var(--plum-deep); }

  .lp-tile:focus-visible {
    outline: 2px solid var(--rose);
    outline-offset: 3px;
  }
  .lp-tile--cream:focus-visible { outline-color: var(--accent); }
  .lp-tile--rose:focus-visible  { outline-color: var(--plum-deep); }

  @media (max-width: 760px) {
    .lp-hero {
      /* +100 px proti 70dvh (~586 → ~686 px na 812 viewportu) per klientovu
         žádost; větší plocha pro hero foto, lepší dojem na 6.1" displejích.
         Horizontální padding ponechán na 0, .lp-hero__inner si bere gutter
         přes width: calc(100% , 2*gutter), aby content alignoval s tile gridem
         a zbytkem stránky (jinak by byl double-guttered). */
      min-height: 85vh;
      min-height: 85dvh;
      padding: var(--space-7) 0;
    }
    .lp-tiles { padding: var(--space-5) 0 var(--space-6); }
    /* Mobile: 2 sloupce (3×2 → 2×3) aby každý tile měl dost šířky pro cross-link
       layout (1fr auto), tj. text vlevo + 32 px arrow kruh vpravo bez overlapu. */
    .lp-tiles__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }
    .lp-tile {
      min-height: 82px;
      padding: 12px 14px;
      gap: 10px;
    }
    .lp-tile__type { max-width: none; gap: 8px; min-width: 0; }
    .lp-tile__title {
      /* Scaluje s šířkou telefonu, ať i nejdelší popisek („Pobytová &
         projektová stipendia") padne do 2 řádků. line-clamp je tvrdý
         strop 2 řádky pro jistotu i na úzkých displejích. */
      font-size: clamp(0.72rem, 3.1vw, 0.875rem);
      line-height: 1.2;
      text-wrap: pretty;
      display: -webkit-box;
      -webkit-box-orient: vertical;
      -webkit-line-clamp: 2;
      overflow: hidden;
    }
    /* Eyebrows na telefonu skryté, titul stačí jako label. */
    .lp-tile__eyebrow {
      display: none;
    }
    .lp-tile__arrow {
      width: 32px;
      height: 32px;
      flex-shrink: 0;
    }
    .lp-tile__ribbon path { stroke-width: 32; }
    .lp-tile--rose .lp-tile__ribbon path  { stroke-width: 14; }
    .lp-tile--deep .lp-tile__ribbon path  { stroke-width: 38; }
    .lp-tile--ink .lp-tile__ribbon path   { stroke-width: 30; }

    /* Telefonní šachovnice: prohozená barevnost Komunitní zázemí (3.)
       a Stipendijní programy (4.) oproti desktopu, aby v 2-col mobile
       gridu vznikla cross diagonal cream/ink, ne sloupcový blok cream
       vlevo + ink vpravo. */
    .lp-tiles__grid > .lp-tile:nth-child(3) {
      background: var(--plum-deep);
      color: var(--cream);
    }
    .lp-tiles__grid > .lp-tile:nth-child(3) .lp-tile__title { color: var(--cream); }
    .lp-tiles__grid > .lp-tile:nth-child(3) .lp-tile__icon { color: var(--rose); }
    .lp-tiles__grid > .lp-tile:nth-child(3) .lp-tile__ribbon path { stroke: var(--rose); }
    .lp-tiles__grid > .lp-tile:nth-child(4) {
      background: var(--cream);
      color: var(--fg);
      border: 1px solid var(--accent-rule);
    }
    .lp-tiles__grid > .lp-tile:nth-child(4) .lp-tile__title { color: var(--fg); }
    .lp-tiles__grid > .lp-tile:nth-child(4) .lp-tile__icon { color: var(--accent); }
    .lp-tiles__grid > .lp-tile:nth-child(4) .lp-tile__ribbon path { stroke: var(--accent); }
    /* Arrow hover/idle flip: na cream → cream svg po vyplnění,
       na ink → plum-deep svg po vyplnění. */
    .lp-tiles__grid > .lp-tile:nth-child(3):hover .lp-tile__arrow :global(svg),
    .lp-tiles__grid > .lp-tile:nth-child(3):focus-visible .lp-tile__arrow :global(svg) {
      color: var(--plum-deep);
    }
    .lp-tiles__grid > .lp-tile:nth-child(4):hover .lp-tile__arrow :global(svg),
    .lp-tiles__grid > .lp-tile:nth-child(4):focus-visible .lp-tile__arrow :global(svg) {
      color: var(--cream);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .lp-tile, .lp-tile__ribbon, .lp-tile__arrow { transition: none; }
    .lp-tile__ribbon[data-ribbon-reveal] path,
    .lp-tile__ribbon[data-ribbon-reveal].is-revealed path {
      stroke-dashoffset: 0;
      animation: none;
    }
    .lp-tile:hover .lp-tile__ribbon { transform: none; }
  }

  /* ============ ZIG-ZAG SEKCE (one-pager) ============ */
  .lp-sections {
    background: var(--bg);
    color: var(--fg);
  }
  .lp-section {
    position: relative;
    padding: var(--space-8) 0;
    scroll-margin-top: 80px;
    overflow: hidden;
  }
  /* Žádné dělící čáry mezi sekcemi , liché/sudé sekce odděluje střídání
     pozadí (sudé dostanou --bg-muted), to vizuálně stačí. */
  .lp-section:nth-of-type(even) {
    background: var(--bg-muted);
  }

  /* Section ribbon: thin Bezier curve as background ornament, fills negative
     space opposite the photo + peeká z pod fotky (Z-layer). Sudé sekce dostanou
     scaleX(-1) takže ribbon přejde na druhou stranu. */
  .lp-section__ribbon {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 0.32;
    transition: transform 600ms var(--ease-quart);
  }
  /* Hover CTA tlačítek lehce posune ribbon doprava (na sudých sekcích doleva
     díky scaleX(-1) flipu). Pohyb je decentní (12 px), dává sekci „dech". */
  .lp-section:has(.lp-section__cta:hover) .lp-section__ribbon,
  .lp-section:has(.lp-section__cta:focus-visible) .lp-section__ribbon {
    transform: translateX(12px);
  }
  .lp-section:nth-of-type(even):has(.lp-section__cta:hover) .lp-section__ribbon,
  .lp-section:nth-of-type(even):has(.lp-section__cta:focus-visible) .lp-section__ribbon {
    transform: scaleX(-1) translateX(12px);
  }
  .lp-section__ribbon svg {
    width: 100%;
    height: 100%;
    display: block;
    overflow: visible;
  }
  .lp-section__ribbon path {
    fill: none;
    stroke: var(--accent);
    stroke-width: 36;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .lp-section__ribbon[data-ribbon-reveal] path {
    stroke-dasharray: 1;
    stroke-dashoffset: 1;
    pathLength: 1;
  }
  /* Reveal + idle breathing. Sekce není klikatelná, takže žádný hover. */
  .lp-section__ribbon[data-ribbon-reveal].is-revealed path {
    animation:
      ribbon-snake-draw 3.2s var(--ease-snake) forwards,
      ribbon-breathe 7s ease-in-out 3.2s infinite;
  }
  .lp-section:nth-of-type(even) .lp-section__ribbon { transform: scaleX(-1); }

  .lp-section__inner {
    position: relative;
    z-index: 2;
    /* Width pattern: content edge se zarovná s tile grid (1480 max,
       gutter-aware na menších viewportech). Žádný vlastní padding-x. */
    width: calc(100% - 2 * var(--gutter));
    max-width: var(--container-wide);
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: clamp(32px, 5vw, 80px);
    align-items: center;
  }
  /* Sudé sekce: flipnout media doleva, text doprava. */
  .lp-section:nth-of-type(even) .lp-section__media { order: -1; }

  .lp-section__text {
    min-width: 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }
  .lp-section__eyebrow {
    font: 600 var(--text-2xs) / 1 var(--font-sans);
    color: var(--accent);
    letter-spacing: var(--tracking-wider);
    text-transform: uppercase;
    margin: 0;
  }
  .lp-section__title {
    font: 600 clamp(1.875rem, 3.5vw, 2.75rem) / 1.15 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    margin: 0;
    max-width: 22ch;
  }
  .lp-section__lede {
    font: 400 var(--text-md) / 1.6 var(--font-sans);
    color: var(--fg-soft, var(--fg));
    margin: 0;
    max-width: 54ch;
  }
  .lp-section__ctas {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-3);
    margin-top: var(--space-2);
  }
  /* Sekce CTA, jednotný outline tón napříč zigzagem (plum border + plum text,
     transparentní pozadí, hover swap na solid plum fill).
     Desktop: row layout (arrow vpravo přes flex order vede z následující řádky).
     Mobile (≤ 760): column layout, arrow nad textem, top-left, menší font. */
  .lp-section__cta {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 12px 18px;
    font: 600 var(--text-sm) / 1 var(--font-sans);
    letter-spacing: var(--tracking-wide);
    border-radius: var(--radius-input);
    text-decoration: none;
    transition: background 200ms ease, color 200ms ease, border-color 200ms ease, transform 200ms ease;
    border: 1px solid var(--accent);
    color: var(--accent);
    background: transparent;
  }
  .lp-section__cta:hover,
  .lp-section__cta:focus-visible {
    background: var(--accent);
    color: var(--accent-on);
    outline: none;
  }
  /* Pravidlo: detail/navigace = jen tah + plum text (default .lp-section__cta);
     CTA akce = fill + bílé písmo (rezervační pruh). Ubytování CTA „Více
     o ubytování" je detail, takže žádný solid override , drží default outline. */
  .lp-section__cta-arrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 1.05em;
    line-height: 1;
    transition: transform 220ms ease;
  }
  .lp-section__cta-label { line-height: 1; }
  .lp-section__cta:hover .lp-section__cta-arrow,
  .lp-section__cta:focus-visible .lp-section__cta-arrow {
    transform: translateX(2px);
  }
  .lp-section__media { min-width: 0; }

  /* Sekce Ubytování: úvodní text je na celou šířku (bez pravé galerie),
     karty jdou pod ním ve dvou sloupcích. */
  .lp-section--intro .lp-section__inner { grid-template-columns: 1fr; }

  /* ============ KARTY POD ÚVODEM UBYTOVÁNÍ ============ */
  /* Dva sloupce (Privátní apartmány, Sdílené pokoje): scroll galerie nahoře,
     pod ní rámované tělo s nadpisem, popisem a tlačítkem Zjistit více. Celá
     karta reaguje na hover (lift + plum rámeček), tlačítko se vyplní. Vychází
     z archivní RoomCard estetiky (starší verze webu). */
  .lp-ucards {
    position: relative;
    z-index: 2;
    width: calc(100% - 2 * var(--gutter));
    max-width: var(--container-wide);
    /* Kladná mezera nad kartami, ať nepřekrývají CTA „Zjistit více" pod úvodem. */
    margin: var(--space-5) auto 0;
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: clamp(20px, 3vw, 40px);
  }
  .lp-ucard {
    display: flex;
    flex-direction: column;
    background: var(--bg);
    border: 1px solid var(--border-muted);
    border-radius: var(--radius-card, 16px);
    overflow: hidden;
    transition: border-color 220ms var(--ease-quart),
                transform 220ms var(--ease-quart),
                box-shadow 220ms var(--ease-quart);
  }
  .lp-ucard:hover,
  .lp-ucard:focus-within {
    border-color: var(--accent);
    transform: translateY(-3px);
    box-shadow: 0 14px 34px rgba(90, 42, 95, 0.13);
  }
  .lp-ucard__media { min-width: 0; }
  .lp-ucard__body {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    padding: clamp(20px, 2vw, 28px);
  }
  .lp-ucard__title {
    margin: 0;
    font: 600 clamp(1.3rem, 1.9vw, 1.7rem) / 1.15 var(--font-sans);
    letter-spacing: var(--tracking-tight);
    color: var(--fg);
  }
  .lp-ucard__title-link {
    color: inherit;
    text-decoration: none;
    transition: color 200ms ease;
  }
  .lp-ucard:hover .lp-ucard__title-link,
  .lp-ucard__title-link:focus-visible { color: var(--accent); }
  .lp-ucard__title-link:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; border-radius: 2px; }
  .lp-ucard__text {
    margin: 0;
    font: 400 var(--text-md) / 1.55 var(--font-sans);
    color: var(--fg-soft, var(--fg));
  }
  .lp-ucard__cta {
    margin-top: auto;
    align-self: flex-start;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 11px 18px;
    font: 700 var(--text-sm) / 1 var(--font-sans);
    letter-spacing: var(--tracking-wide);
    border-radius: var(--radius-input);
    border: 1px solid var(--accent);
    color: var(--accent);
    background: transparent;
    text-decoration: none;
    transition: background 200ms ease, color 200ms ease;
  }
  .lp-ucard:hover .lp-ucard__cta,
  .lp-ucard__cta:focus-visible {
    background: var(--accent);
    color: var(--accent-on);
    outline: none;
  }
  .lp-ucard__cta:focus-visible { outline: 2px solid var(--accent); outline-offset: 3px; }
  .lp-ucard__cta-arrow {
    display: inline-flex;
    align-items: center;
    transition: transform 200ms var(--ease-quart);
  }
  .lp-ucard:hover .lp-ucard__cta-arrow,
  .lp-ucard__cta:focus-visible .lp-ucard__cta-arrow { transform: translateX(3px); }
  @media (max-width: 680px) {
    .lp-ucards { grid-template-columns: 1fr; }
  }
  @media (prefers-reduced-motion: reduce) {
    .lp-ucard,
    .lp-ucard__title-link,
    .lp-ucard__cta,
    .lp-ucard__cta-arrow { transition: none; }
  }

  @media (max-width: 760px) {
    /* Na mobilu uvolnit overflow, aby mobile section divider (z kit.css)
       mohl přesahovat hranici sekce. Ribbon-clipping přesouváme na vnitřní
       wrapper, kde to potřebuje zůstat. Border-bottom přebíjíme, divider
       z kit.css ::before slouží jako hranice mezi sekcemi a 1 px ruler by
       se s ním vizuálně bil. */
    .lp-section {
      padding: var(--space-7) 0;
      overflow: visible;
      border-bottom: 0;
    }
    .lp-section__ribbon { overflow: hidden; opacity: 0.32; }
    .lp-section__inner {
      grid-template-columns: 1fr;
      gap: var(--space-5);
    }
    /* Na mobilu vždy text první, pak média (žádný flip). */
    .lp-section:nth-of-type(even) .lp-section__media { order: 0; }
    .lp-section__ribbon path { stroke-width: 32; }

    /* CTA na mobilu: arrow + label v jednom inline rowu, menší font. */
    .lp-section__ctas { flex-direction: column; align-items: flex-start; }
    .lp-section__cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 12px 16px;
      font-size: 13px;
      letter-spacing: 0;
    }
    .lp-section__cta-arrow {
      font-size: 16px;
      line-height: 1;
    }
    .lp-section__cta-label { line-height: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .lp-section__ribbon[data-ribbon-reveal] path,
    .lp-section__ribbon[data-ribbon-reveal].is-revealed path {
      stroke-dashoffset: 0;
      animation: none;
    }
  }

  /* ResidentBenefits pruh „vše v ceně" uvnitř Living sekce, pod textem a CTA.
     Vlastní padding má ResidentBenefits sám, wrap jen přidává margin-top.
     Background transparent override: ať ribbon sekce protéká i pod chip karty. */
  .lp-section__benefits {
    position: relative;
    z-index: 2;
    margin-top: var(--space-5);
  }
  .lp-section__benefits :global(.rb) {
    background: transparent;
    padding-top: 0;
    padding-bottom: 0;
  }

  /* Leaflet brand overrides: same plum accent, sharp borders. */
  :global(.leaflet-container) {
    background: var(--k-05);
    font-family: var(--font-sans);
    z-index: 0;
  }
  :global(.leaflet-control-attribution) {
    background: rgba(255, 255, 255, 0.92) !important;
    color: var(--fg-muted) !important;
    font-size: 10px !important;
    border-top: 1px solid var(--border-muted) !important;
    padding: 2px 8px !important;
  }
  :global(.leaflet-control-attribution a) { color: var(--fg) !important; }
  :global(.leaflet-control-zoom a) {
    background: var(--bg) !important;
    color: var(--fg-muted) !important;
    border-color: var(--border-muted) !important;
    font-family: var(--font-sans);
  }
  :global(.leaflet-control-zoom a:hover) {
    background: var(--accent) !important;
    color: #fff !important;
    border-color: var(--accent) !important;
  }
  :global(.lp-map-popup-wrap .leaflet-popup-content-wrapper) {
    background: var(--bg);
    border: 1px solid var(--accent);
    box-shadow: 0 8px 32px hsl(var(--hue) 39% 27% / 0.18);
    border-radius: var(--radius-input);
    padding: 0;
  }
  :global(.lp-map-popup-wrap .leaflet-popup-tip) { background: var(--bg); border: 1px solid var(--accent); }
  :global(.lp-map-popup-wrap .leaflet-popup-content) { margin: 0; width: auto !important; min-width: 200px; }
  :global(.lp-map-popup-wrap .leaflet-popup-close-button) { color: var(--fg-subtle) !important; padding: 6px 8px 0 0 !important; }
  :global(.lp-map-popup-wrap .leaflet-popup-close-button:hover) { color: var(--accent) !important; }
  :global(.lp-map-popup) {
    display: grid; gap: 4px;
    padding: 14px 16px;
    color: var(--k-100);
    font: 500 13px / 1.5 var(--font-sans);
    text-decoration: none; cursor: pointer;
  }
  :global(.lp-map-popup strong) { font-weight: 600; color: var(--k-100); letter-spacing: 0.02em; }
  :global(.lp-map-popup .lp-map-popup__cta) { color: var(--accent); font-weight: 600; margin-top: 4px; }
</style>
